requests>=2.28.0
rich>=12.0.0
urllib3<2.0.0
matplotlib>=3.5.0
pyyaml>=6.0

# Optional — only needed for AI features (optimizer.py ask / ai.summary_in_report)
# Ollama (local, free): no package required — just install https://ollama.com
# anthropic>=0.40.0   # for --backend claude
# openai>=1.0.0       # for --backend openai
